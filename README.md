# CPS Smart Home V2 Documentation

Open `index.html` for English or `zh.html` for Chinese. The documentation is a standalone HTML/CSS site and does not require a build command.

Before public submission, replace the placeholders in **Source, support & ownership** with the final GitHub URL, release tag, author details, contact method, and license. Add final Docker image information only after Docker support is implemented and verified.
